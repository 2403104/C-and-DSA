#include <bits/stdc++.h>
using namespace std;
int main(){
    // first convert to the string then use
    // bitset<8>b(29);//(00011101) binary representatin of 29 till 8 bits(use all the operation of string )
    // int setBit=b.count();//it counts the number of set bits 
    // bool anySetBit=b.any();//check if there is any set bit
    // bool allSetBit=b.all();//checks that all the bits are set or not
    // b.set(1);//set bit at 1
    // b.reset(0);//reset bit at 0
    // b.flip(1);//toggle the bit at 1


    // unsigned int x=29;
    // cout << "Popcount: " << __builtin_popcount(x) << endl; // Count set bits
    // cout << "Leading Zeros: " << __builtin_clz(x) << endl;  // Count leading zeros
    // cout << "Trailing Zeros: " << __builtin_ctz(x) << endl; // Count trailing zeros
    // cout << "Parity: " << __builtin_parity(x) << endl;      // Even/Odd count of set bits

    // bitset<8> b("1101");  // Binary: 00001101
    // cout << "Bitset: " << b << endl;
    // cout << "As String: " << b.to_string() << endl;
    // cout << "As Unsigned Long: " << b.to_ulong() << endl;
    // cout << "Bit at position 3: " << b.test(3) << endl;
    // cout << "Size of bitset: " << b.size() << endl;

    // b <<= 2;  // Left shift by 2
    // cout << "After left shift: " << b << endl;

















    return 0;
}